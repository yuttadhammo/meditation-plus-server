{% for item in chats %}
    <div class="chat-row">
        <span class="chat-time">{{ item.time|date() }}</span>
        <span class="chat-user">{{ item.user.username }}:</span>
        <span class="chat-message">{{ item.message }}</span>
    </div>
{% endfor %}